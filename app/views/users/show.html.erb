<h1>WELCOME <%=@user.email%> </h1>

	<table class="table table-condensed">
		<tr>
        	<th>nombre</th>
	        <th>descripcion</th>
	        <th></th>
	        <th></th>
      	</tr>
 
      	<% @recetas.each do |receta| %>
	    <tr>
	      <td><%= link_to receta.nombre, receta_path(receta) %></td>
	      <td><%= receta.descipcion %></td>
	      <td><%= link_to 'Edit', edit_receta_path(receta) %></td>
	      <td><%= link_to 'Delete', receta_path(receta), :confirm => 'Are you sure?', :method => :delete %></td>
	    </tr>
    <% end %>
    </table>
<%= link_to "crear receta",  new_receta_path%>