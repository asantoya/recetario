<h1> BIENVENIDO A SU APP DE RECETARIOS </h1>


<% if current_user %>
<%= link_to "my profile",user_path(current_user) %>
<%else%>
<%= link_to "sign in", new_user_session_path %>
<%end%>

<div id="recetas_chart" style="width: 560px; height: 300px;"></div>

<table>
	<tr>
    	<th>nombre</th>
        <th></th>
        
  	</tr>

  	<% @recetas.each do |receta| %>
    <tr>
      <td><%= receta.nombre %></td>
      <td><%= link_to 'ver', receta_path(receta) %></td>
      </tr>
<% end %>
</table>